<style is:global>
  .hidden,
  .tnx-lb-modal.hidden {
    display: none !important;
  }

  .tnx-modal-open {
    overflow: hidden;
  }

  .tnx-lb-row {
    position: relative;
    display: grid;
    grid-template-columns: 96px minmax(0, 1fr) auto;
    align-items: center;
    gap: 1rem;
    width: 100%;
    padding: 1rem 1.15rem;
    border-radius: 1.4rem;
    border: 1px solid rgba(194, 146, 0, 0.16);
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.025), rgba(255, 255, 255, 0.01)),
      rgba(0, 0, 0, 0.34);
    cursor: pointer;
    overflow: hidden;
    transition:
      transform 0.22s ease,
      border-color 0.22s ease,
      box-shadow 0.22s ease;
  }

  .tnx-lb-row:hover,
  .tnx-lb-row:focus-visible {
    transform: translateY(-2px);
    border-color: rgba(255, 218, 106, 0.34);
    box-shadow:
      0 12px 36px rgba(0, 0, 0, 0.28),
      0 0 22px rgba(255, 218, 106, 0.08);
    outline: none;
  }

  .tnx-lb-row-rank {
    display: inline-flex;
    min-height: 54px;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    border: 1px solid rgba(194, 146, 0, 0.22);
    background: rgba(255, 218, 106, 0.05);
    color: #ffda6a;
    font-weight: 950;
  }

  .tnx-lb-user {
    display: flex;
    min-width: 0;
    align-items: center;
    gap: 0.85rem;
  }

  .tnx-lb-avatar {
    display: inline-flex;
    width: 52px;
    height: 52px;
    flex: 0 0 52px;
    align-items: center;
    justify-content: center;
    border-radius: 1rem;
    border: 1px solid rgba(194, 146, 0, 0.22);
    background:
      radial-gradient(circle at 30% 30%, rgba(255, 218, 106, 0.16), transparent 65%),
      rgba(255, 255, 255, 0.03);
    color: #ffda6a;
    font-weight: 950;
  }

  .tnx-lb-user-main {
    display: grid;
    min-width: 0;
    gap: 0.16rem;
  }

  .tnx-lb-user-name {
    margin: 0;
    overflow: hidden;
    color: #fff;
    font-weight: 900;
    line-height: 1.35;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .tnx-lb-user-login {
    margin: 0;
    overflow: hidden;
    color: rgb(148, 163, 184);
    direction: ltr;
    font-size: 0.82rem;
    font-weight: 800;
    text-align: left;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .tnx-lb-row-value {
    color: #fff;
    font-weight: 950;
    text-align: right;
    white-space: nowrap;
  }

  .tnx-lb-row--rank-1,
  .tnx-lb-row--rank-2,
  .tnx-lb-row--rank-3 {
    border-color: rgba(255, 218, 106, 0.34);
  }

  .tnx-lb-row--rank-1 {
    background:
      linear-gradient(90deg, rgba(255, 218, 106, 0.18), rgba(255, 218, 106, 0.05) 35%, rgba(0, 0, 0, 0.32)),
      rgba(0, 0, 0, 0.28);
  }

  .tnx-lb-row--rank-2 {
    background:
      linear-gradient(90deg, rgba(255, 215, 140, 0.14), rgba(255, 218, 106, 0.04) 35%, rgba(0, 0, 0, 0.32)),
      rgba(0, 0, 0, 0.28);
  }

  .tnx-lb-row--rank-3 {
    background:
      linear-gradient(90deg, rgba(226, 180, 96, 0.12), rgba(255, 218, 106, 0.035) 35%, rgba(0, 0, 0, 0.32)),
      rgba(0, 0, 0, 0.28);
  }

  .tnx-lb-row--rank-1 .tnx-lb-row-rank {
    animation: tnx-first-row-pulse 2.8s ease-in-out infinite;
  }

  @keyframes tnx-first-row-pulse {
    0%,
    100% {
      transform: scale(1);
      box-shadow: 0 0 0 0 rgba(255, 218, 106, 0.18);
    }

    50% {
      transform: scale(1.035);
      box-shadow: 0 0 0 10px rgba(255, 218, 106, 0);
    }
  }

  .tnx-lb-elite-divider {
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0.75rem 0;
    padding: 0.75rem 1rem;
    border-radius: 999px;
    border: 1px dashed rgba(255, 218, 106, 0.22);
    background: rgba(255, 218, 106, 0.045);
    color: #ffda6a;
    font-size: 0.82rem;
    font-weight: 950;
    text-align: center;
  }

  .tnx-lb-empty {
    display: grid;
    min-height: 220px;
    place-items: center;
    gap: 0.8rem;
    padding: 2rem;
    border-radius: 1.5rem;
    border: 1px solid rgba(194, 146, 0, 0.18);
    background:
      radial-gradient(circle at top, rgba(255, 218, 106, 0.08), transparent 58%),
      rgba(0, 0, 0, 0.26);
    text-align: center;
  }

  .tnx-lb-empty iconify-icon {
    color: #ffda6a;
    font-size: 2.7rem;
  }

  .tnx-lb-empty strong {
    color: #fff;
    font-size: 1.05rem;
    font-weight: 950;
  }

  .tnx-lb-empty span {
    color: rgb(156, 163, 175);
    font-size: 0.9rem;
    font-weight: 800;
    line-height: 1.7;
  }

  .tnx-lb-top-card {
    position: relative;
    display: grid;
    justify-items: center;
    gap: 0.7rem;
    padding: 1.35rem 1rem 1.2rem;
    border-radius: 1.5rem;
    border: 1px solid rgba(194, 146, 0, 0.18);
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.03), rgba(255, 255, 255, 0.012)),
      rgba(0, 0, 0, 0.28);
    text-align: center;
    cursor: pointer;
    overflow: hidden;
    transition: 0.22s ease;
  }

  .tnx-lb-top-card:hover,
  .tnx-lb-top-card:focus-visible {
    transform: translateY(-3px);
    border-color: rgba(255, 218, 106, 0.36);
    box-shadow:
      0 14px 30px rgba(0, 0, 0, 0.28),
      0 0 24px rgba(255, 218, 106, 0.12);
    outline: none;
  }

  .tnx-lb-top-card .tnx-lb-rank {
    position: absolute;
    top: 0.9rem;
    inset-inline-start: 0.9rem;
    display: inline-flex;
    min-width: 58px;
    min-height: 34px;
    align-items: center;
    justify-content: center;
    padding: 0.32rem 0.8rem;
    border-radius: 999px;
    border: 1px solid rgba(194, 146, 0, 0.22);
    background: rgba(255, 218, 106, 0.07);
    color: #ffda6a;
    font-size: 0.78rem;
    font-weight: 950;
  }

  .tnx-lb-medal {
    display: inline-flex;
    width: 66px;
    height: 66px;
    align-items: center;
    justify-content: center;
    border-radius: 1.3rem;
    border: 1px solid rgba(194, 146, 0, 0.22);
    background:
      radial-gradient(circle at 30% 30%, rgba(255, 218, 106, 0.18), transparent 65%),
      rgba(255, 255, 255, 0.03);
    color: #ffda6a;
    font-size: 1.8rem;
  }

  .tnx-lb-top-card--first .tnx-lb-medal {
    animation: tnx-first-pulse 2.6s ease-in-out infinite;
  }

  @keyframes tnx-first-pulse {
    0%,
    100% {
      transform: scale(1);
      box-shadow: 0 0 0 0 rgba(255, 218, 106, 0.2);
    }

    50% {
      transform: scale(1.04);
      box-shadow: 0 0 0 10px rgba(255, 218, 106, 0);
    }
  }

  .tnx-lb-top-name {
    margin: 0;
    color: #fff;
    font-size: 1.06rem;
    font-weight: 950;
    line-height: 1.35;
    word-break: break-word;
  }

  .tnx-lb-top-login {
    color: rgb(148, 163, 184);
    direction: ltr;
    font-size: 0.82rem;
    font-weight: 800;
  }

  .tnx-lb-top-value {
    margin: 0;
    color: #ffda6a;
    font-size: 0.95rem;
    font-weight: 950;
  }

  .tnx-lb-modal {
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1rem;
    background: rgba(0, 0, 0, 0.74);
    backdrop-filter: blur(10px);
  }

  .tnx-lb-modal-card {
    width: min(100%, 1180px);
    max-height: calc(100vh - 2rem);
    overflow: auto;
    border-radius: 1.9rem;
    border: 1px solid rgba(194, 146, 0, 0.22);
    background:
      radial-gradient(circle at top, rgba(255, 218, 106, 0.07), transparent 48%),
      rgba(10, 10, 12, 0.98);
    padding: 1.35rem;
    box-shadow:
      0 28px 80px rgba(0, 0, 0, 0.54),
      0 0 34px rgba(255, 218, 106, 0.08);
  }

  .tnx-lb-modal-topbar {
    display: flex;
    justify-content: flex-start;
    margin-bottom: 0.85rem;
  }

  .tnx-lb-modal-close {
    display: inline-flex;
    width: 46px;
    height: 46px;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    border: 1px solid rgba(194, 146, 0, 0.24);
    background: rgba(255, 255, 255, 0.03);
    color: #ffda6a;
    font-size: 1.25rem;
    cursor: pointer;
  }

  .tnx-lb-modal-layout {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(290px, 390px);
    gap: 1.25rem;
    align-items: stretch;
  }

  .tnx-lb-info-side {
    order: 1;
  }

  .tnx-lb-preview-side {
    order: 2;
  }

  .tnx-lb-preview-side,
  .tnx-lb-preview-box {
    display: flex;
    min-width: 0;
    align-items: center;
    justify-content: center;
  }

  .tnx-lb-preview-box {
    width: 100%;
    border-radius: 1.65rem;
    border: 1px solid rgba(194, 146, 0, 0.16);
    background: rgba(0, 0, 0, 0.28);
    padding: 1rem;
  }

  #share-card-canvas {
    display: block;
    width: 100%;
    max-width: 360px;
    height: auto;
    border-radius: 1.35rem;
    box-shadow:
      0 0 0 1px rgba(255, 218, 106, 0.1),
      0 0 26px rgba(255, 218, 106, 0.08);
  }

  .tnx-lb-info-side {
    display: grid;
    min-width: 0;
    align-content: start;
    gap: 1rem;
    border-radius: 1.65rem;
    border: 1px solid rgba(194, 146, 0, 0.16);
    background: rgba(0, 0, 0, 0.24);
    padding: 1.1rem;
  }

  .tnx-lb-modal-head {
    display: grid;
    grid-template-columns: 92px minmax(0, 1fr);
    align-items: center;
    gap: 1rem;
  }

  .tnx-lb-modal-avatar {
    display: inline-flex;
    width: 92px;
    height: 92px;
    align-items: center;
    justify-content: center;
    border-radius: 1.45rem;
    border: 1px solid rgba(194, 146, 0, 0.28);
    background:
      radial-gradient(circle at 30% 30%, rgba(255, 218, 106, 0.18), transparent 65%),
      rgba(255, 255, 255, 0.03);
    color: #ffda6a;
    font-size: 2.2rem;
    font-weight: 950;
  }

  .tnx-lb-modal-name-wrap {
    display: grid;
    min-width: 0;
    gap: 0.35rem;
    text-align: right;
  }

  .tnx-lb-modal-board {
    color: #ffda6a;
    font-size: 0.9rem;
    font-weight: 900;
  }

  .tnx-lb-modal-name {
    color: #fff;
    font-size: clamp(1.7rem, 3vw, 2.6rem);
    font-weight: 950;
    line-height: 1.1;
    word-break: break-word;
  }

  .tnx-lb-modal-login {
    color: rgb(148, 163, 184);
    direction: ltr;
    font-size: 0.9rem;
    font-weight: 800;
    text-align: right;
  }

  .tnx-lb-stats-grid,
  .tnx-lb-modal-actions {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.9rem;
  }

  .tnx-lb-stat-box {
    min-width: 0;
    border-radius: 1.25rem;
    border: 1px solid rgba(194, 146, 0, 0.18);
    background: rgba(0, 0, 0, 0.26);
    padding: 1rem;
    text-align: center;
  }

  .tnx-lb-stat-box span {
    display: block;
    margin-bottom: 0.45rem;
    color: rgb(148, 163, 184);
    font-size: 0.9rem;
    font-weight: 800;
  }

  .tnx-lb-stat-box strong {
    display: block;
    overflow-wrap: anywhere;
    color: #ffda6a;
    font-size: clamp(1.3rem, 2.4vw, 2rem);
    font-weight: 950;
    line-height: 1.1;
  }

  .tnx-lb-card-mode-control,
  .tnx-lb-compare-box {
    border-radius: 1.3rem;
    border: 1px solid rgba(194, 146, 0, 0.18);
    background: rgba(0, 0, 0, 0.22);
    padding: 1rem;
  }

  .tnx-lb-card-mode-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .tnx-lb-card-mode-copy {
    display: grid;
    gap: 0.3rem;
  }

  .tnx-lb-card-mode-copy strong {
    color: #fff;
    font-size: 0.95rem;
    font-weight: 950;
  }

  .tnx-lb-card-mode-copy span,
  .tnx-lb-card-mode-hint {
    color: rgb(156, 163, 175);
    font-size: 0.82rem;
    font-weight: 800;
    line-height: 1.7;
  }

  .tnx-lb-card-mode-side {
    display: inline-flex;
    align-items: center;
    gap: 0.8rem;
  }

  .tnx-lb-card-mode-status {
    min-width: 82px;
    border-radius: 999px;
    border: 1px solid rgba(194, 146, 0, 0.25);
    background: rgba(255, 218, 106, 0.08);
    color: #ffda6a;
    padding: 0.4rem 0.85rem;
    font-size: 0.8rem;
    font-weight: 950;
    text-align: center;
  }

  .tnx-lb-switch {
    position: relative;
    width: 74px;
    height: 40px;
    border-radius: 999px;
    border: 1px solid rgba(194, 146, 0, 0.3);
    background: rgba(255, 255, 255, 0.06);
    cursor: pointer;
  }

  .tnx-lb-switch-thumb {
    position: absolute;
    top: 5px;
    right: 5px;
    width: 28px;
    height: 28px;
    border-radius: 999px;
    background: #c5c7cf;
    transition: 0.25s ease;
  }

  .tnx-lb-switch.is-on {
    border-color: rgba(255, 218, 106, 0.68);
    background: linear-gradient(180deg, rgba(255, 218, 106, 0.28), rgba(194, 146, 0, 0.24));
  }

  .tnx-lb-switch.is-on .tnx-lb-switch-thumb {
    right: calc(100% - 33px);
    background: linear-gradient(180deg, #ffea99, #ffda6a);
  }

  .tnx-lb-btn {
    display: inline-flex;
    min-height: 52px;
    align-items: center;
    justify-content: center;
    border-radius: 1rem;
    border: 1px solid rgba(194, 146, 0, 0.24);
    background: rgba(255, 218, 106, 0.08);
    color: #ffda6a;
    padding: 0.8rem 1.2rem;
    font-size: 0.95rem;
    font-weight: 950;
    cursor: pointer;
  }

  .tnx-lb-btn--solid {
    background: linear-gradient(180deg, rgba(255, 218, 106, 0.92), rgba(194, 146, 0, 0.92));
    color: #111;
  }

  .tnx-lb-compare-strip {
    display: flex;
    width: 100%;
    min-height: 58px;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    border-radius: 1rem;
    border: 1px solid rgba(194, 146, 0, 0.24);
    background:
      radial-gradient(circle at top right, rgba(255, 218, 106, 0.1), transparent 42%),
      rgba(0, 0, 0, 0.28);
    color: #fff;
    padding: 0.85rem 1rem;
    text-align: right;
    cursor: pointer;
  }

  .tnx-lb-compare-strip-text {
    display: grid;
    gap: 0.15rem;
  }

  .tnx-lb-compare-strip-text strong {
    color: #ffda6a;
    font-size: 0.95rem;
    font-weight: 950;
  }

  .tnx-lb-compare-strip-text small {
    color: rgb(156, 163, 175);
    font-size: 0.78rem;
    font-weight: 800;
  }

  .tnx-lb-compare-strip-icon {
    display: inline-flex;
    width: 2.2rem;
    height: 2.2rem;
    flex: 0 0 2.2rem;
    align-items: center;
    justify-content: center;
    border-radius: 0.8rem;
    border: 1px solid rgba(194, 146, 0, 0.28);
    background: rgba(255, 218, 106, 0.07);
    color: #ffda6a;
    transition: transform 0.22s ease;
  }

  .tnx-lb-compare-accordion-body {
    display: none;
    margin-top: 0;
  }

  .tnx-lb-compare-accordion-root.is-open .tnx-lb-compare-accordion-body {
    display: grid;
    margin-top: 0.85rem;
  }

  .tnx-lb-compare-accordion-root.is-open .tnx-lb-compare-strip-icon {
    transform: rotate(180deg);
  }

  .tnx-lb-compare-box {
    display: grid;
    gap: 0.8rem;
  }

  .tnx-lb-compare-controls {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 0.8rem;
  }

  .tnx-lb-compare-controls input {
    width: 100%;
    min-width: 0;
    height: 52px;
    border-radius: 1rem;
    border: 1px solid rgba(194, 146, 0, 0.18);
    background: rgba(0, 0, 0, 0.26);
    color: #fff;
    padding: 0 1rem;
    font-size: 0.95rem;
    font-weight: 800;
    outline: none;
    text-align: right;
  }

  .tnx-lb-compare-result {
    min-height: 76px;
    border-radius: 1rem;
    border: 1px dashed rgba(194, 146, 0, 0.18);
    background: rgba(0, 0, 0, 0.18);
    color: rgb(156, 163, 175);
    padding: 1rem;
    font-size: 0.92rem;
    font-weight: 800;
    line-height: 1.8;
    text-align: center;
  }

  .tnx-lb-compare-grid {
    display: grid;
    grid-template-columns: 1fr minmax(150px, 210px) 1fr;
    gap: 0.8rem;
  }

  .tnx-lb-compare-card,
  .tnx-lb-compare-diff {
    display: grid;
    place-items: center;
    border-radius: 1rem;
    border: 1px solid rgba(194, 146, 0, 0.15);
    background: rgba(255, 255, 255, 0.02);
    padding: 1rem;
    text-align: center;
  }

  .tnx-lb-compare-card strong,
  .tnx-lb-compare-diff strong {
    color: #fff;
    font-weight: 950;
  }

  .tnx-lb-compare-card span,
  .tnx-lb-compare-diff span {
    color: #ffda6a;
    font-size: 0.9rem;
    font-weight: 900;
  }

  [data-reveal] {
    opacity: 0;
    transform: translateY(16px);
    transition:
      opacity 0.55s ease,
      transform 0.55s ease;
  }

  [data-reveal].is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  @media (max-width: 980px) {
    .tnx-lb-modal-layout {
      grid-template-columns: 1fr;
    }

    .tnx-lb-info-side {
      order: 1;
    }

    .tnx-lb-preview-side {
      order: 2;
    }

    #share-card-canvas {
      max-width: 320px;
    }
  }

  @media (max-width: 900px) {
    .tnx-lb-compare-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .tnx-lb-row {
      grid-template-columns: 78px minmax(0, 1fr);
    }

    .tnx-lb-row-value {
      grid-column: 2;
      justify-self: start;
    }

    .tnx-lb-modal-head,
    .tnx-lb-stats-grid,
    .tnx-lb-modal-actions,
    .tnx-lb-compare-controls {
      grid-template-columns: 1fr;
    }

    .tnx-lb-modal-name-wrap,
    .tnx-lb-modal-login {
      text-align: center;
    }
  }
</style>

<script is:inline>
  ;(() => {
    const cleanupKey = '__tnxLeaderboardsCleanup'

    if (window[cleanupKey]) {
      window[cleanupKey]()
    }

    const perPage = 20

    const boardConfig = {
      points: {
        title: 'النقاط',
        heading: 'ترتيب النقاط',
        subtitle: 'أكثر الأعضاء نقاطًا',
        description: 'أكثر الأعضاء نقاطًا في المجتمع.',
        icon: 'tabler:coins',
        unit: 'نقطة',
        emptyTitle: 'ما فيه بيانات نقاط',
        emptyText: 'تأكد أن ملف leaderboard_data.json يحتوي على points.',
      },
      gold: {
        title: 'الذهب',
        heading: 'ترتيب الذهب',
        subtitle: 'ترتيب الذهب',
        description: 'أكثر الأعضاء امتلاكًا للذهب.',
        icon: 'tabler:coin',
        unit: 'ذهب',
        emptyTitle: 'الذهب غير مفعل حاليًا',
        emptyText: 'القسم جاهز، وبيظهر الترتيب إذا بدأت تسجل بيانات gold.',
      },
      watchtime: {
        title: 'المشاهدة',
        heading: 'ترتيب المشاهدة',
        subtitle: 'أكثر الأعضاء مشاهدة',
        description: 'أكثر الأعضاء حضورًا ومشاهدة.',
        icon: 'tabler:clock-hour-4',
        unit: '',
        emptyTitle: 'ما فيه بيانات مشاهدة',
        emptyText: 'تأكد أن ملف leaderboard_data.json يحتوي على watchtime.',
      },
    }

    let activeBoard = 'points'
    let searchQuery = ''
    let activeModalUser = null
    let shareCardMode = 'single'

    let allData = {
      points: [],
      gold: [],
      watchtime: [],
    }

    let filteredData = {
      points: [],
      gold: [],
      watchtime: [],
    }

    let visibleCount = {
      points: perPage,
      gold: perPage,
      watchtime: perPage,
    }

    let revealObserver = null
    const listeners = []

    function addListener(target, event, handler, options) {
      if (!target) return
      target.addEventListener(event, handler, options)
      listeners.push(() => target.removeEventListener(event, handler, options))
    }

    window[cleanupKey] = () => {
      listeners.forEach((cleanup) => cleanup())
      listeners.length = 0

      if (revealObserver) {
        revealObserver.disconnect()
        revealObserver = null
      }

      document.body.classList.remove('tnx-modal-open')
    }

    function escapeHtml(value) {
      return String(value ?? '')
        .replaceAll('&', '&amp;')
        .replaceAll('<', '&lt;')
        .replaceAll('>', '&gt;')
        .replaceAll('"', '&quot;')
        .replaceAll("'", '&#039;')
    }

    function toNumber(value) {
      if (typeof value === 'number') {
        return Number.isFinite(value) ? value : 0
      }

      const number = Number(
        String(value ?? '')
          .replaceAll(',', '')
          .replace(/[^\d.-]/g, '')
      )

      return Number.isFinite(number) ? number : 0
    }

    function formatNumber(value) {
      return new Intl.NumberFormat('en-US').format(toNumber(value))
    }

    function formatCardDate(date = new Date()) {
      const day = String(date.getDate()).padStart(2, '0')
      const month = String(date.getMonth() + 1).padStart(2, '0')
      const year = String(date.getFullYear())
      return `${day}/${month}/${year}`
    }

    function getDisplayName(user) {
      return String(
        user?.UserName ||
        user?.UserLogin ||
        user?.username ||
        user?.name ||
        'Unknown'
      ).trim()
    }

    function getLogin(user) {
      return String(user?.UserLogin || user?.login || '').trim()
    }

    function getValue(user) {
      return user?.Value ?? user?.value ?? user?.points ?? user?.score ?? 0
    }

    function isNumericValue(value) {
      if (typeof value === 'number') return Number.isFinite(value)
      const raw = String(value ?? '').trim()
      return Boolean(raw) && /^-?[\d,]+(\.\d+)?$/.test(raw)
    }

    function formatValue(boardId, value) {
      const raw = String(value ?? '').trim()

      if (boardId === 'watchtime') {
        return raw || '0'
      }

      if (isNumericValue(value)) {
        return `${formatNumber(value)} ${boardConfig[boardId].unit}`.trim()
      }

      return `${raw || '0'} ${boardConfig[boardId].unit}`.trim()
    }

    function getAvatarLetter(name) {
      const clean = String(name || '').trim()
      return clean ? clean.charAt(0).toUpperCase() : '?'
    }

    function makeUserId(boardId, user, index) {
      const login = getLogin(user)
      const name = getDisplayName(user)
      return `${boardId}-${index + 1}-${String(login || name).toLowerCase()}`
    }

    function normalizeBoardData(data, boardId) {
      const list = Array.isArray(data?.[boardId]) ? data[boardId] : []

      return list
        .filter((user) => user && (user.UserName || user.UserLogin || user.username || user.name))
        .map((user) => ({
          ...user,
          __board: boardId,
          __name: getDisplayName(user),
          __login: getLogin(user),
          __value: getValue(user),
          __number: toNumber(getValue(user)),
        }))
        .sort((a, b) => b.__number - a.__number)
        .map((user, index) => ({
          ...user,
          __rank: index + 1,
          __id: makeUserId(boardId, user, index),
        }))
    }

    function findMatchingUserInBoard(boardId, sourceUser) {
      if (!sourceUser) return null

      const users = allData[boardId] || []
      const sourceLogin = String(sourceUser.__login || '').trim().toLowerCase()
      const sourceName = String(sourceUser.__name || '').trim().toLowerCase()

      if (sourceLogin) {
        const byLogin = users.find((user) => String(user.__login || '').trim().toLowerCase() === sourceLogin)
        if (byLogin) return byLogin
      }

      if (sourceName) {
        const byName = users.find((user) => String(user.__name || '').trim().toLowerCase() === sourceName)
        if (byName) return byName
      }

      return null
    }

    function getCombinedCardStats(user) {
      const pointsUser = findMatchingUserInBoard('points', user)
      const watchtimeUser = findMatchingUserInBoard('watchtime', user)

      return {
        pointsRank: pointsUser ? `#${pointsUser.__rank}` : '--',
        pointsValue: pointsUser ? formatValue('points', pointsUser.__value) : '--',
        watchtimeRank: watchtimeUser ? `#${watchtimeUser.__rank}` : '--',
        watchtimeValue: watchtimeUser ? formatValue('watchtime', watchtimeUser.__value) : '--',
      }
    }

    function getElements() {
      return {
        tabs: Array.from(document.querySelectorAll('[data-board-tab]')),
        panels: Array.from(document.querySelectorAll('[data-board-panel]')),
        search: document.getElementById('leaderboard-search') || document.getElementById('search'),
        boardHeading: document.getElementById('board-heading'),
        boardDescription: document.getElementById('board-description'),
        boardCount: document.getElementById('board-count'),
        activeBoardTitle: document.getElementById('active-board-title'),
        activeBoardSubtitle: document.getElementById('active-board-subtitle'),
        activeBoardPill: document.getElementById('active-board-pill'),
        activeBoardIcon: document.getElementById('active-board-icon'),
        topThreeList: document.getElementById('top-three-list'),

        containers: {
          points: document.getElementById('points-list'),
          gold: document.getElementById('gold-list'),
          watchtime: document.getElementById('watchtime-list'),
        },

        loadMoreButtons: {
          points: document.querySelector('[data-load-more="points"]') || document.getElementById('loadMorePoints'),
          gold: document.querySelector('[data-load-more="gold"]') || document.getElementById('loadMoreGold'),
          watchtime: document.querySelector('[data-load-more="watchtime"]') || document.getElementById('loadMoreWatchtime'),
        },

        showLessButtons: {
          points: document.querySelector('[data-show-less="points"]') || document.getElementById('showLessPoints'),
          gold: document.querySelector('[data-show-less="gold"]') || document.getElementById('showLessGold'),
          watchtime: document.querySelector('[data-show-less="watchtime"]') || document.getElementById('showLessWatchtime'),
        },
      }
    }

    function ensureModalShell() {
      let modal = document.getElementById('leaderboard-user-modal')

      if (!modal) {
        modal = document.createElement('div')
        modal.id = 'leaderboard-user-modal'
        document.body.appendChild(modal)
      }

      modal.className = 'tnx-lb-modal hidden'
      modal.setAttribute('aria-hidden', 'true')

      modal.innerHTML = `
        <div class="tnx-lb-modal-card" dir="rtl" role="dialog" aria-modal="true" aria-label="بطاقة العضو">
          <div class="tnx-lb-modal-topbar">
            <button type="button" class="tnx-lb-modal-close" data-close-user-modal aria-label="إغلاق">×</button>
          </div>

          <div class="tnx-lb-modal-layout">
            <aside class="tnx-lb-preview-side">
              <div class="tnx-lb-preview-box">
                <canvas id="share-card-canvas" width="1080" height="1350"></canvas>
              </div>
            </aside>

            <section class="tnx-lb-info-side">
              <div class="tnx-lb-modal-head">
                <div id="modal-user-avatar" class="tnx-lb-modal-avatar">?</div>

                <div class="tnx-lb-modal-name-wrap">
                  <div id="modal-board-label" class="tnx-lb-modal-board">ترتيب النقاط</div>
                  <div id="modal-user-name" class="tnx-lb-modal-name">---</div>
                  <div id="modal-user-login" class="tnx-lb-modal-login"></div>
                </div>
              </div>

              <div class="tnx-lb-stats-grid">
                <div class="tnx-lb-stat-box">
                  <span>المركز</span>
                  <strong id="modal-user-rank">--</strong>
                </div>

                <div class="tnx-lb-stat-box">
                  <span>القيمة</span>
                  <strong id="modal-user-value">--</strong>
                </div>
              </div>

              <div class="tnx-lb-card-mode-control">
                <div class="tnx-lb-card-mode-row">
                  <div class="tnx-lb-card-mode-copy">
                    <strong>نوع البطاقة</strong>
                    <span>تقدر تبدّل بين بطاقة منفصلة أو بطاقة مدمجة.</span>
                  </div>

                  <div class="tnx-lb-card-mode-side">
                    <button
                      type="button"
                      id="share-card-mode-switch"
                      class="tnx-lb-switch"
                      role="switch"
                      aria-checked="false"
                      aria-label="تبديل نوع البطاقة"
                    >
                      <span class="tnx-lb-switch-thumb"></span>
                    </button>

                    <span id="share-card-mode-status" class="tnx-lb-card-mode-status">منفصلة</span>
                  </div>
                </div>

                <div id="share-card-mode-hint" class="tnx-lb-card-mode-hint">
                  الآن البطاقة تعرض ترتيب وقيمة التبويب الحالي فقط.
                </div>
              </div>

              <div class="tnx-lb-modal-actions">
                <button type="button" id="native-share-card" class="tnx-lb-btn">مشاركة</button>
                <button type="button" id="download-share-card" class="tnx-lb-btn tnx-lb-btn--solid">
                  تحميل البطاقة PNG
                </button>
              </div>

              <div id="tnx-compare-accordion-root" class="tnx-lb-compare-accordion-root">
                <button
                  type="button"
                  id="tnx-compare-accordion-toggle"
                  class="tnx-lb-compare-strip"
                  aria-expanded="false"
                  aria-controls="tnx-compare-accordion-body"
                >
                  <span class="tnx-lb-compare-strip-text">
                    <strong>قارن هذا العضو مع شخص ثاني</strong>
                    <small>اضغط لفتح المقارنة</small>
                  </span>

                  <span class="tnx-lb-compare-strip-icon" aria-hidden="true">
                    <iconify-icon icon="tabler:chevron-down"></iconify-icon>
                  </span>
                </button>

                <div id="tnx-compare-accordion-body" class="tnx-lb-compare-accordion-body">
                  <div class="tnx-lb-compare-box">
                    <div class="tnx-lb-compare-controls">
                      <button type="button" id="compare-button" class="tnx-lb-btn tnx-lb-btn--solid">قارن</button>

                      <input
                        id="compare-target-input"
                        type="text"
                        list="leaderboard-users-list"
                        placeholder="اكتب اسم العضو الثاني"
                        autocomplete="off"
                      />
                    </div>

                    <div id="compare-result" class="tnx-lb-compare-result">
                      اختر عضو ثاني عشان تظهر المقارنة.
                    </div>
                  </div>
                </div>
              </div>

              <datalist id="leaderboard-users-list"></datalist>
            </section>
          </div>
        </div>
      `

      return modal
    }

    function getModalElements() {
      return {
        modal: document.getElementById('leaderboard-user-modal'),
        modalUserAvatar: document.getElementById('modal-user-avatar'),
        modalUserName: document.getElementById('modal-user-name'),
        modalUserLogin: document.getElementById('modal-user-login'),
        modalUserRank: document.getElementById('modal-user-rank'),
        modalUserValue: document.getElementById('modal-user-value'),
        modalBoardLabel: document.getElementById('modal-board-label'),

        compareRoot: document.getElementById('tnx-compare-accordion-root'),
        compareToggle: document.getElementById('tnx-compare-accordion-toggle'),
        compareBody: document.getElementById('tnx-compare-accordion-body'),
        compareTargetInput: document.getElementById('compare-target-input'),
        compareResult: document.getElementById('compare-result'),
        usersDatalist: document.getElementById('leaderboard-users-list'),

        shareCanvas: document.getElementById('share-card-canvas'),
        shareCardModeSwitch: document.getElementById('share-card-mode-switch'),
        shareCardModeStatus: document.getElementById('share-card-mode-status'),
        shareCardModeHint: document.getElementById('share-card-mode-hint'),
      }
    }

    function syncShareCardModeUI() {
      const elements = getModalElements()
      const isCombined = shareCardMode === 'combined'

      elements.shareCardModeSwitch?.classList.toggle('is-on', isCombined)
      elements.shareCardModeSwitch?.setAttribute('aria-checked', String(isCombined))

      if (elements.shareCardModeStatus) {
        elements.shareCardModeStatus.textContent = isCombined ? 'مدمجة' : 'منفصلة'
      }

      if (elements.shareCardModeHint) {
        elements.shareCardModeHint.textContent = isCombined
          ? 'الآن البطاقة تعرض النقاط والمشاهدة معًا في نفس الصورة.'
          : 'الآن البطاقة تعرض ترتيب وقيمة التبويب الحالي فقط.'
      }
    }

    function setCompareAccordionOpen(open) {
      const elements = getModalElements()

      if (!elements.compareRoot || !elements.compareToggle) return

      elements.compareRoot.classList.toggle('is-open', open)
      elements.compareToggle.setAttribute('aria-expanded', String(open))
    }

    function updateUserDatalist() {
      const elements = getModalElements()
      const users = allData[activeBoard] || []

      if (!elements.usersDatalist) return

      elements.usersDatalist.innerHTML = users
        .slice(0, 600)
        .map((user) => {
          const label = `#${user.__rank} — ${user.__name}`
          return `<option value="${escapeHtml(user.__name)}">${escapeHtml(label)}</option>`
        })
        .join('')
    }

    function initScrollReveal() {
      const revealElements = Array.from(document.querySelectorAll('[data-reveal]'))

      if (revealElements.length === 0) return

      if (!('IntersectionObserver' in window)) {
        revealElements.forEach((element) => element.classList.add('is-visible'))
        return
      }

      if (revealObserver) {
        revealObserver.disconnect()
      }

      revealObserver = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (!entry.isIntersecting) return
            entry.target.classList.add('is-visible')
            revealObserver.unobserve(entry.target)
          })
        },
        {
          threshold: 0,
          rootMargin: '260px 0px 260px 0px',
        }
      )

      revealElements.forEach((element) => {
        if (!element.classList.contains('is-visible')) {
          revealObserver.observe(element)
        }
      })

      forceRevealVisibleElements()
      window.requestAnimationFrame(forceRevealVisibleElements)
      window.setTimeout(forceRevealVisibleElements, 120)
    }

    function forceRevealVisibleElements() {
      const buffer = 260
      const viewportHeight = window.innerHeight || document.documentElement.clientHeight

      document.querySelectorAll('[data-reveal]:not(.is-visible)').forEach((element) => {
        const rect = element.getBoundingClientRect()

        if (rect.top <= viewportHeight + buffer && rect.bottom >= -buffer) {
          element.classList.add('is-visible')

          if (revealObserver) {
            revealObserver.unobserve(element)
          }
        }
      })
    }

    function renderEmptyState(container, boardId, customTitle, customText) {
      const config = boardConfig[boardId]

      container.innerHTML = `
        <div class="tnx-lb-empty" data-reveal="up">
          <iconify-icon icon="tabler:database-off"></iconify-icon>
          <strong>${escapeHtml(customTitle || config.emptyTitle)}</strong>
          <span>${escapeHtml(customText || config.emptyText)}</span>
        </div>
      `

      initScrollReveal()
    }

    function getRankSpecialClass(rank) {
      if (rank === 1) return 'tnx-lb-row--rank-1'
      if (rank === 2) return 'tnx-lb-row--rank-2'
      if (rank === 3) return 'tnx-lb-row--rank-3'
      return ''
    }

    function renderEliteDivider() {
      return `
        <div class="tnx-lb-elite-divider" data-reveal="up">
          نهاية النخبة — بعد المركز 10
        </div>
      `
    }

    function renderRow(user, boardId) {
      const name = user.__name
      const login = user.__login
      const showLogin = login && login.toLowerCase() !== name.toLowerCase()

      return `
        <article
          class="tnx-lb-row ${getRankSpecialClass(user.__rank)}"
          data-reveal="up"
          data-open-user="${escapeHtml(user.__id)}"
          data-board-id="${escapeHtml(boardId)}"
          tabindex="0"
          role="button"
          aria-label="فتح بطاقة ${escapeHtml(name)}"
        >
          <div class="tnx-lb-row-rank">#${user.__rank}</div>

          <div class="tnx-lb-user">
            <div class="tnx-lb-avatar">${escapeHtml(getAvatarLetter(name))}</div>

            <div class="tnx-lb-user-main">
              <p class="tnx-lb-user-name" title="${escapeHtml(name)}">${escapeHtml(name)}</p>
              ${
                showLogin
                  ? `<p class="tnx-lb-user-login" title="${escapeHtml(login)}">@${escapeHtml(login)}</p>`
                  : ''
              }
            </div>
          </div>

          <div class="tnx-lb-row-value">${escapeHtml(formatValue(boardId, user.__value))}</div>
        </article>
      `
    }

    function renderBoard(boardId) {
      const elements = getElements()
      const container = elements.containers[boardId]
      const loadMoreButton = elements.loadMoreButtons[boardId]
      const showLessButton = elements.showLessButtons[boardId]
      const users = filteredData[boardId] || []

      if (!container) return

      if (users.length === 0) {
        const hasSearch = searchQuery.length > 0

        renderEmptyState(
          container,
          boardId,
          hasSearch ? 'ما حصلت العضو' : undefined,
          hasSearch ? 'جرّب تكتب الاسم أو اليوزر بطريقة ثانية.' : undefined
        )

        loadMoreButton?.classList.add('hidden')
        showLessButton?.classList.add('hidden')
        return
      }

      const visibleUsers = users.slice(0, visibleCount[boardId])

      container.innerHTML = visibleUsers
        .map((user) => {
          const divider = !searchQuery && user.__rank === 11 ? renderEliteDivider() : ''
          return `${divider}${renderRow(user, boardId)}`
        })
        .join('')

      loadMoreButton?.classList.toggle('hidden', visibleCount[boardId] >= users.length)
      showLessButton?.classList.toggle('hidden', visibleCount[boardId] <= perPage)

      initScrollReveal()
    }

    function renderAllBoards() {
      Object.keys(boardConfig).forEach((boardId) => renderBoard(boardId))
    }

    function updateBoardMeta() {
      const elements = getElements()
      const config = boardConfig[activeBoard]
      const count = filteredData[activeBoard]?.length || 0
      const totalCount = allData[activeBoard]?.length || 0

      if (elements.boardHeading) elements.boardHeading.textContent = config.heading

      if (elements.boardDescription) {
        elements.boardDescription.textContent = searchQuery
          ? `نتائج البحث تعرض الرتبة الأصلية من أصل ${formatNumber(totalCount)} عضو.`
          : config.description
      }

      if (elements.boardCount) elements.boardCount.textContent = formatNumber(count)
      if (elements.activeBoardTitle) elements.activeBoardTitle.textContent = config.title

      if (elements.activeBoardSubtitle) {
        elements.activeBoardSubtitle.textContent = searchQuery
          ? 'نتائج البحث بالترتيب الحقيقي'
          : config.subtitle
      }

      if (elements.activeBoardPill) {
        elements.activeBoardPill.textContent = searchQuery ? 'بحث بالترتيب الحقيقي' : config.heading
      }

      if (elements.activeBoardIcon) {
        elements.activeBoardIcon.setAttribute('icon', config.icon)
      }
    }

    function renderTopThree() {
      const elements = getElements()
      const container = elements.topThreeList
      const users = searchQuery ? filteredData[activeBoard] || [] : allData[activeBoard] || []

      if (!container) return

      if (users.length === 0) {
        const config = boardConfig[activeBoard]

        container.innerHTML = `
          <article class="tnx-lb-top-card tnx-lb-top-card--first" data-reveal="up">
            <span class="tnx-lb-rank">TOP</span>
            <div class="tnx-lb-medal">
              <iconify-icon icon="tabler:trophy"></iconify-icon>
            </div>
            <p class="tnx-lb-top-name">${escapeHtml(searchQuery ? 'ما حصلت العضو' : config.emptyTitle)}</p>
            <p class="tnx-lb-top-value">${escapeHtml(searchQuery ? 'جرّب تبحث باسم ثاني' : config.emptyText)}</p>
          </article>
        `

        initScrollReveal()
        return
      }

      const medals = ['tabler:crown', 'tabler:medal-2', 'tabler:medal']

      container.innerHTML = users
        .slice(0, 3)
        .map((user, index) => {
          const name = user.__name
          const login = user.__login
          const showLogin = login && login.toLowerCase() !== name.toLowerCase()

          return `
            <article
              class="tnx-lb-top-card${index === 0 ? ' tnx-lb-top-card--first' : ''}"
              data-reveal="up"
              data-open-user="${escapeHtml(user.__id)}"
              data-board-id="${escapeHtml(activeBoard)}"
              tabindex="0"
              role="button"
              aria-label="فتح بطاقة ${escapeHtml(name)}"
            >
              <span class="tnx-lb-rank">#${user.__rank}</span>

              <div class="tnx-lb-medal">
                <iconify-icon icon="${medals[index] || 'tabler:user'}"></iconify-icon>
              </div>

              <p class="tnx-lb-top-name" title="${escapeHtml(name)}">${escapeHtml(name)}</p>

              ${
                showLogin
                  ? `<span class="tnx-lb-top-login">@${escapeHtml(login)}</span>`
                  : ''
              }

              <p class="tnx-lb-top-value">${escapeHtml(formatValue(activeBoard, user.__value))}</p>
            </article>
          `
        })
        .join('')

      initScrollReveal()
    }

    function setActiveBoard(boardId) {
      if (!boardConfig[boardId]) return

      activeBoard = boardId

      const elements = getElements()

      elements.tabs.forEach((tab) => {
        const isActive = tab.dataset.boardTab === boardId
        tab.classList.toggle('is-active', isActive)
        tab.setAttribute('aria-selected', String(isActive))
      })

      elements.panels.forEach((panel) => {
        panel.classList.toggle('is-active', panel.dataset.boardPanel === boardId)
      })

      updateBoardMeta()
      renderTopThree()
      updateUserDatalist()
      renderBoard(boardId)
      initScrollReveal()
    }

    function handleSearch(query) {
      searchQuery = String(query || '').trim().toLowerCase()

      Object.keys(boardConfig).forEach((boardId) => {
        filteredData[boardId] = allData[boardId].filter((user) => {
          const name = String(user.__name || '').toLowerCase()
          const login = String(user.__login || '').toLowerCase()
          return name.includes(searchQuery) || login.includes(searchQuery)
        })

        visibleCount[boardId] = perPage
      })

      renderAllBoards()
      updateBoardMeta()
      renderTopThree()
      initScrollReveal()
    }

    function findUserById(userId, boardId = activeBoard) {
      const direct = (allData[boardId] || []).find((user) => user.__id === userId)
      if (direct) return direct

      for (const key of Object.keys(boardConfig)) {
        const user = (allData[key] || []).find((item) => item.__id === userId)
        if (user) return user
      }

      return null
    }

    function findUserByInput(value) {
      const query = String(value || '').trim().toLowerCase()
      if (!query) return null

      const users = allData[activeBoard] || []

      return (
        users.find((user) => user.__name.toLowerCase() === query) ||
        users.find((user) => user.__login.toLowerCase() === query) ||
        users.find((user) => user.__name.toLowerCase().includes(query)) ||
        users.find((user) => user.__login.toLowerCase().includes(query)) ||
        null
      )
    }

    function openUserModal(user, boardId = activeBoard) {
      const modal = ensureModalShell()
      const elements = getModalElements()
      const realBoard = user.__board || boardId || activeBoard
      const config = boardConfig[realBoard] || boardConfig.points

      activeModalUser = user
      activeBoard = realBoard

      updateUserDatalist()
      syncShareCardModeUI()
      setCompareAccordionOpen(false)

      if (elements.modalUserAvatar) elements.modalUserAvatar.textContent = getAvatarLetter(user.__name)
      if (elements.modalUserName) elements.modalUserName.textContent = user.__name

      if (elements.modalUserLogin) {
        const showLogin = user.__login && user.__login.toLowerCase() !== user.__name.toLowerCase()
        elements.modalUserLogin.textContent = showLogin ? `@${user.__login}` : ''
      }

      if (elements.modalUserRank) elements.modalUserRank.textContent = `#${user.__rank}`
      if (elements.modalUserValue) elements.modalUserValue.textContent = formatValue(activeBoard, user.__value)
      if (elements.modalBoardLabel) elements.modalBoardLabel.textContent = config.heading

      if (elements.compareTargetInput) elements.compareTargetInput.value = ''
      if (elements.compareResult) elements.compareResult.textContent = 'اختر عضو ثاني عشان تظهر المقارنة.'

      renderShareCard(user)

      modal.classList.remove('hidden')
      modal.setAttribute('aria-hidden', 'false')
      document.body.classList.add('tnx-modal-open')

      setCompareAccordionOpen(false)
    }

    function closeUserModal() {
      const elements = getModalElements()

      activeModalUser = null
      setCompareAccordionOpen(false)

      if (elements.modal) {
        elements.modal.classList.add('hidden')
        elements.modal.setAttribute('aria-hidden', 'true')
      }

      document.body.classList.remove('tnx-modal-open')
    }

    function renderCompare() {
      const elements = getModalElements()

      if (!activeModalUser || !elements.compareTargetInput || !elements.compareResult) return

      const targetUser = findUserByInput(elements.compareTargetInput.value)

      if (!targetUser) {
        elements.compareResult.innerHTML = `
          <div>
            <strong style="display:block;color:#fff">ما حصلت العضو الثاني</strong>
            <span style="display:block;margin-top:.35rem;color:rgb(107,114,128)">اكتب الاسم أو اليوزر بشكل أوضح.</span>
          </div>
        `
        return
      }

      if (targetUser.__id === activeModalUser.__id) {
        elements.compareResult.innerHTML = `
          <div>
            <strong style="display:block;color:#fff">اختر عضو مختلف</strong>
            <span style="display:block;margin-top:.35rem;color:rgb(107,114,128)">ما تقدر تقارن الشخص بنفسه.</span>
          </div>
        `
        return
      }

      const valueDiff = Math.abs(activeModalUser.__number - targetUser.__number)
      const rankDiff = Math.abs(activeModalUser.__rank - targetUser.__rank)
      const betterRankUser = activeModalUser.__rank < targetUser.__rank ? activeModalUser : targetUser
      const higherValueUser = activeModalUser.__number >= targetUser.__number ? activeModalUser : targetUser

      elements.compareResult.innerHTML = `
        <div class="tnx-lb-compare-grid">
          <div class="tnx-lb-compare-card">
            <strong>${escapeHtml(activeModalUser.__name)}</strong>
            <span>${escapeHtml(formatValue(activeBoard, activeModalUser.__value))}</span>
            <small>المركز #${activeModalUser.__rank}</small>
          </div>

          <div class="tnx-lb-compare-diff">
            <div>
              <strong>${escapeHtml(formatValue(activeBoard, valueDiff))}</strong>
              <span>فرق القيمة</span>

              <strong style="margin-top:.8rem">${formatNumber(rankDiff)}</strong>
              <span>فرق المراكز</span>

              <span style="margin-top:.8rem">الأعلى ترتيبًا: ${escapeHtml(betterRankUser.__name)}</span>
              <span>الأعلى قيمة: ${escapeHtml(higherValueUser.__name)}</span>
            </div>
          </div>

          <div class="tnx-lb-compare-card">
            <strong>${escapeHtml(targetUser.__name)}</strong>
            <span>${escapeHtml(formatValue(activeBoard, targetUser.__value))}</span>
            <small>المركز #${targetUser.__rank}</small>
          </div>
        </div>
      `
    }

    function drawRoundedRect(ctx, x, y, width, height, radius) {
      ctx.beginPath()
      ctx.moveTo(x + radius, y)
      ctx.lineTo(x + width - radius, y)
      ctx.quadraticCurveTo(x + width, y, x + width, y + radius)
      ctx.lineTo(x + width, y + height - radius)
      ctx.quadraticCurveTo(x + width, y + height, x + width - radius, y + height)
      ctx.lineTo(x + radius, y + height)
      ctx.quadraticCurveTo(x, y + height, x, y + height - radius)
      ctx.lineTo(x, y + radius)
      ctx.quadraticCurveTo(x, y, x + radius, y)
      ctx.closePath()
    }

    function drawTextFit(ctx, text, x, y, maxWidth, fontSize, fontFamily, weight = '900') {
      let size = fontSize
      ctx.font = `${weight} ${size}px ${fontFamily}`

      while (ctx.measureText(text).width > maxWidth && size > 34) {
        size -= 2
        ctx.font = `${weight} ${size}px ${fontFamily}`
      }

      ctx.fillText(text, x, y)
    }

    function drawMetricBox(ctx, options) {
      const {
        x,
        y,
        width,
        height,
        title,
        rank,
        value,
        font,
      } = options

      ctx.fillStyle = 'rgba(0, 0, 0, 0.42)'
      drawRoundedRect(ctx, x, y, width, height, 34)
      ctx.fill()

      ctx.strokeStyle = 'rgba(255, 218, 106, 0.26)'
      ctx.lineWidth = 3
      drawRoundedRect(ctx, x, y, width, height, 34)
      ctx.stroke()

      ctx.direction = 'rtl'
      ctx.textAlign = 'center'

      ctx.fillStyle = 'rgba(255, 255, 255, 0.68)'
      ctx.font = `800 30px ${font}`
      ctx.fillText(title, x + width / 2, y + 58)

      ctx.fillStyle = '#ffda6a'
      ctx.font = `900 58px ${font}`
      ctx.fillText(rank, x + width / 2, y + 132)

      ctx.fillStyle = '#ffffff'
      drawTextFit(ctx, value, x + width / 2, y + 196, width - 52, 38, font, '900')
    }

    function drawDate(ctx, width, height, font, cardDate) {
      ctx.direction = 'ltr'
      ctx.textAlign = 'left'

      ctx.fillStyle = 'rgba(255, 255, 255, 0.42)'
      ctx.font = `800 24px ${font}`
      ctx.fillText('DATE', 92, height - 130)

      ctx.fillStyle = 'rgba(255, 255, 255, 0.68)'
      ctx.font = `900 30px ${font}`
      ctx.fillText(cardDate, 92, height - 96)

      ctx.direction = 'rtl'
      ctx.textAlign = 'center'
    }

    function renderSingleShareCard(ctx, user, width, height, font, cardDate) {
      const config = boardConfig[activeBoard] || boardConfig.points

      ctx.fillStyle = '#ffda6a'
      ctx.font = `900 54px ${font}`
      ctx.fillText('TNX6 COMMUNITY', width / 2, 180)

      ctx.fillStyle = 'rgba(255, 255, 255, 0.78)'
      ctx.font = `800 34px ${font}`
      ctx.fillText(config.heading, width / 2, 238)

      if (!user) {
        ctx.fillStyle = '#ffffff'
        ctx.font = `900 58px ${font}`
        ctx.fillText('ما فيه بيانات', width / 2, 620)
        drawDate(ctx, width, height, font, cardDate)
        return
      }

      ctx.fillStyle = 'rgba(0, 0, 0, 0.42)'
      drawRoundedRect(ctx, 140, 315, width - 280, 650, 46)
      ctx.fill()

      ctx.strokeStyle = 'rgba(255, 218, 106, 0.28)'
      ctx.lineWidth = 3
      drawRoundedRect(ctx, 140, 315, width - 280, 650, 46)
      ctx.stroke()

      ctx.fillStyle = '#ffda6a'
      ctx.font = `900 86px ${font}`
      ctx.fillText(`#${user.__rank}`, width / 2, 450)

      ctx.fillStyle = '#ffffff'
      drawTextFit(ctx, user.__name, width / 2, 565, width - 260, 76, font, '900')

      if (user.__login && user.__login.toLowerCase() !== user.__name.toLowerCase()) {
        ctx.fillStyle = 'rgba(255, 255, 255, 0.46)'
        ctx.font = `800 34px ${font}`
        ctx.direction = 'ltr'
        ctx.fillText(`@${user.__login}`, width / 2, 620)
        ctx.direction = 'rtl'
      }

      ctx.fillStyle = '#ffda6a'
      ctx.font = `900 60px ${font}`
      ctx.fillText(formatValue(activeBoard, user.__value), width / 2, 740)

      ctx.fillStyle = 'rgba(255, 255, 255, 0.68)'
      ctx.font = `800 30px ${font}`
      ctx.fillText('مركزك داخل مجتمع TNX6', width / 2, 805)

      ctx.strokeStyle = 'rgba(255, 218, 106, 0.22)'
      ctx.lineWidth = 2
      ctx.beginPath()
      ctx.moveTo(230, 860)
      ctx.lineTo(width - 230, 860)
      ctx.stroke()

      ctx.fillStyle = 'rgba(255, 255, 255, 0.78)'
      ctx.font = `800 32px ${font}`
      ctx.fillText('tnx6.xyz', width / 2, 930)

      ctx.fillStyle = '#ffda6a'
      ctx.font = `900 42px ${font}`
      ctx.fillText('شارك بطاقتك وورنا مركزك', width / 2, 1115)

      ctx.fillStyle = 'rgba(255, 255, 255, 0.38)'
      ctx.font = `700 26px ${font}`
      ctx.fillText('Generated from TNX6 Leaderboards', width / 2, 1190)

      drawDate(ctx, width, height, font, cardDate)
    }

    function renderCombinedShareCard(ctx, user, width, height, font, cardDate) {
      const combinedStats = getCombinedCardStats(user)

      ctx.fillStyle = '#ffda6a'
      ctx.font = `900 54px ${font}`
      ctx.fillText('TNX6 COMMUNITY', width / 2, 180)

      ctx.fillStyle = 'rgba(255, 255, 255, 0.78)'
      ctx.font = `800 34px ${font}`
      ctx.fillText('بطاقة المجتمع', width / 2, 238)

      if (!user) {
        ctx.fillStyle = '#ffffff'
        ctx.font = `900 58px ${font}`
        ctx.fillText('ما فيه بيانات', width / 2, 620)
        drawDate(ctx, width, height, font, cardDate)
        return
      }

      ctx.fillStyle = 'rgba(0, 0, 0, 0.42)'
      drawRoundedRect(ctx, 140, 315, width - 280, 320, 46)
      ctx.fill()

      ctx.strokeStyle = 'rgba(255, 218, 106, 0.28)'
      ctx.lineWidth = 3
      drawRoundedRect(ctx, 140, 315, width - 280, 320, 46)
      ctx.stroke()

      ctx.fillStyle = '#ffffff'
      drawTextFit(ctx, user.__name, width / 2, 445, width - 260, 82, font, '900')

      if (user.__login && user.__login.toLowerCase() !== user.__name.toLowerCase()) {
        ctx.fillStyle = 'rgba(255, 255, 255, 0.46)'
        ctx.font = `800 34px ${font}`
        ctx.direction = 'ltr'
        ctx.fillText(`@${user.__login}`, width / 2, 505)
        ctx.direction = 'rtl'
      }

      ctx.fillStyle = 'rgba(255, 255, 255, 0.66)'
      ctx.font = `800 30px ${font}`
      ctx.fillText('نقاطك ومشاهدتك في مجتمع TNX6', width / 2, 575)

      drawMetricBox(ctx, {
        x: 130,
        y: 700,
        width: 390,
        height: 250,
        title: 'المشاهدة',
        rank: combinedStats.watchtimeRank,
        value: combinedStats.watchtimeValue,
        font,
      })

      drawMetricBox(ctx, {
        x: 560,
        y: 700,
        width: 390,
        height: 250,
        title: 'النقاط',
        rank: combinedStats.pointsRank,
        value: combinedStats.pointsValue,
        font,
      })

      ctx.strokeStyle = 'rgba(255, 218, 106, 0.22)'
      ctx.lineWidth = 2
      ctx.beginPath()
      ctx.moveTo(230, 1010)
      ctx.lineTo(width - 230, 1010)
      ctx.stroke()

      ctx.fillStyle = 'rgba(255, 255, 255, 0.78)'
      ctx.font = `800 32px ${font}`
      ctx.fillText('tnx6.xyz', width / 2, 1080)

      ctx.fillStyle = '#ffda6a'
      ctx.font = `900 42px ${font}`
      ctx.fillText('شارك بطاقتك وورنا مركزك', width / 2, 1155)

      ctx.fillStyle = 'rgba(255, 255, 255, 0.38)'
      ctx.font = `700 26px ${font}`
      ctx.fillText('Generated from TNX6 Leaderboards', width / 2, 1218)

      drawDate(ctx, width, height, font, cardDate)
    }

    function renderShareCard(user = activeModalUser) {
      const elements = getModalElements()
      const canvas = elements.shareCanvas

      if (!canvas) return

      const ctx = canvas.getContext('2d')
      if (!ctx) return

      const width = canvas.width
      const height = canvas.height
      const font = 'Arial, Tahoma, sans-serif'
      const cardDate = formatCardDate()

      ctx.clearRect(0, 0, width, height)

      const bg = ctx.createLinearGradient(0, 0, width, height)
      bg.addColorStop(0, '#09050f')
      bg.addColorStop(0.45, '#05070d')
      bg.addColorStop(1, '#120905')

      ctx.fillStyle = bg
      ctx.fillRect(0, 0, width, height)

      const glow = ctx.createRadialGradient(width * 0.75, height * 0.18, 0, width * 0.75, height * 0.18, 620)
      glow.addColorStop(0, 'rgba(255, 218, 106, 0.30)')
      glow.addColorStop(1, 'rgba(255, 218, 106, 0)')

      ctx.fillStyle = glow
      ctx.fillRect(0, 0, width, height)

      ctx.strokeStyle = 'rgba(255, 218, 106, 0.45)'
      ctx.lineWidth = 6
      drawRoundedRect(ctx, 52, 52, width - 104, height - 104, 54)
      ctx.stroke()

      ctx.direction = 'rtl'
      ctx.textAlign = 'center'

      if (shareCardMode === 'combined') {
        renderCombinedShareCard(ctx, user, width, height, font, cardDate)
      } else {
        renderSingleShareCard(ctx, user, width, height, font, cardDate)
      }
    }

    function getCardFilePrefix() {
      return shareCardMode === 'combined'
        ? 'tnx6-community-card'
        : `tnx6-${activeBoard}-card`
    }

    function downloadShareCard() {
      const elements = getModalElements()
      const canvas = elements.shareCanvas
      const user = activeModalUser

      if (!canvas || !user) return

      renderShareCard(user)

      const safeName = user.__name.replace(/[^\w\u0600-\u06FF-]+/g, '-')
      const link = document.createElement('a')

      link.download = `${getCardFilePrefix()}-${safeName}.png`
      link.href = canvas.toDataURL('image/png')
      link.click()
    }

    function shareCardNative() {
      const elements = getModalElements()
      const canvas = elements.shareCanvas
      const user = activeModalUser

      if (!canvas || !user) return

      renderShareCard(user)

      canvas.toBlob(async (blob) => {
        if (!blob) return

        const safeName = user.__name.replace(/[^\w\u0600-\u06FF-]+/g, '-')
        const file = new File([blob], `${getCardFilePrefix()}-${safeName}.png`, {
          type: 'image/png',
        })

        if (navigator.canShare && navigator.canShare({ files: [file] })) {
          try {
            await navigator.share({
              files: [file],
              title: shareCardMode === 'combined' ? 'TNX6 Community Card' : 'TNX6 Rank Card',
              text: shareCardMode === 'combined'
                ? 'بطاقتي في مجتمع TNX6'
                : `مركزي في TNX6: #${user.__rank}`,
            })
            return
          } catch {
            // fallback to download
          }
        }

        downloadShareCard()
      }, 'image/png')
    }

    async function loadData() {
      const elements = getElements()

      try {
        const response = await fetch(`/api/leaderboard_data.json?site=${Date.now()}`, {
          cache: 'no-store',
        })

        if (!response.ok) {
          throw new Error('Failed to load leaderboard data')
        }

        const data = await response.json()

        allData = {
          points: normalizeBoardData(data, 'points'),
          gold: normalizeBoardData(data, 'gold'),
          watchtime: normalizeBoardData(data, 'watchtime'),
        }

        filteredData = {
          points: [...allData.points],
          gold: [...allData.gold],
          watchtime: [...allData.watchtime],
        }

        renderAllBoards()
        setActiveBoard(activeBoard)
        updateUserDatalist()
        initScrollReveal()
      } catch {
        Object.keys(boardConfig).forEach((boardId) => {
          const container = elements.containers[boardId]
          if (!container) return

          renderEmptyState(
            container,
            boardId,
            'تعذر تحميل الترتيب',
            'تأكد أن /api/leaderboard_data.json موجود ويرجع JSON صحيح.'
          )
        })

        renderTopThree()
        initScrollReveal()
      }
    }

    function bindEvents() {
      const elements = getElements()

      elements.tabs.forEach((tab) => {
        addListener(tab, 'click', () => {
          setActiveBoard(tab.dataset.boardTab)
        })
      })

      Object.keys(boardConfig).forEach((boardId) => {
        addListener(elements.loadMoreButtons[boardId], 'click', () => {
          visibleCount[boardId] += perPage
          renderBoard(boardId)
        })

        addListener(elements.showLessButtons[boardId], 'click', () => {
          visibleCount[boardId] = Math.max(perPage, visibleCount[boardId] - perPage)
          renderBoard(boardId)
        })
      })

      addListener(elements.search, 'input', (event) => {
        handleSearch(event.target.value)
      })

      addListener(document, 'click', (event) => {
        const target = event.target

        if (!(target instanceof Element)) return

        if (target.closest('[data-close-user-modal]')) {
          closeUserModal()
          return
        }

        if (target.closest('#tnx-compare-accordion-toggle')) {
          const modalElements = getModalElements()
          const isOpen = modalElements.compareRoot?.classList.contains('is-open')
          setCompareAccordionOpen(!isOpen)
          return
        }

        if (target.closest('#share-card-mode-switch')) {
          shareCardMode = shareCardMode === 'combined' ? 'single' : 'combined'
          syncShareCardModeUI()
          renderShareCard(activeModalUser)
          return
        }

        if (target.closest('#compare-button')) {
          renderCompare()
          return
        }

        if (target.closest('#download-share-card')) {
          downloadShareCard()
          return
        }

        if (target.closest('#native-share-card')) {
          shareCardNative()
          return
        }

        const userTrigger = target.closest('[data-open-user]')
        if (userTrigger) {
          const userId = userTrigger.getAttribute('data-open-user')
          const boardId = userTrigger.getAttribute('data-board-id') || activeBoard
          const user = findUserById(userId, boardId)

          if (user) {
            openUserModal(user, boardId)
          }

          return
        }

        const modal = document.getElementById('leaderboard-user-modal')
        if (modal && target === modal) {
          closeUserModal()
        }
      })

      addListener(document, 'keydown', (event) => {
        if (event.key === 'Escape') {
          closeUserModal()
          return
        }

        const target = event.target
        if (!(target instanceof Element)) return

        if (target.closest('#compare-target-input') && event.key === 'Enter') {
          renderCompare()
          return
        }

        if (target.closest('[data-open-user]') && event.key === 'Enter') {
          const userId = target.closest('[data-open-user]').getAttribute('data-open-user')
          const boardId = target.closest('[data-open-user]').getAttribute('data-board-id') || activeBoard
          const user = findUserById(userId, boardId)

          if (user) {
            openUserModal(user, boardId)
          }
        }
      })

      addListener(window, 'scroll', forceRevealVisibleElements, { passive: true })
      addListener(window, 'resize', forceRevealVisibleElements)
    }

    function initLeaderboards() {
      ensureModalShell()
      bindEvents()
      updateBoardMeta()
      initScrollReveal()
      loadData()

      window.requestAnimationFrame(initScrollReveal)
      window.setTimeout(initScrollReveal, 300)
      window.setTimeout(initScrollReveal, 900)
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initLeaderboards, { once: true })
    } else {
      initLeaderboards()
    }

    document.addEventListener('astro:page-load', () => {
      if (!document.querySelector('[data-board-tab]') && !document.getElementById('points-list')) return

      if (window[cleanupKey]) {
        window[cleanupKey]()
      }

      initLeaderboards()
    })
  })()
</script>
