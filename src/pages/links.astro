---
import Layout from '~/layouts/PageLayout.astro'
import { Icon } from 'astro-icon/components'

const metadata = {
  title: 'TNX6 — الروابط',
}

const year = new Date().getFullYear()
---

<Layout metadata={metadata}>
  <main class="min-h-[100vh] flex items-center justify-center px-6 py-12 text-white">
    <div class="w-full max-w-xl mx-auto text-center">

      <!-- الصورة + الاسم -->
      <div class="flex flex-col items-center gap-4 mb-10">
        <div class="tnx-ring-links">
          <img
            src="/assets/images/tnx-min.jpg"
            alt="TNX Avatar"
            class="w-full h-full object-cover rounded-full"
            loading="lazy"
          />
        </div>

        <div>
          <h1 class="text-2xl font-extrabold tracking-wide">
            TNX6
          </h1>
          <p class="mt-1 text-sm text-gray-300">
            ستريمر يحب القصص، السوالف، وتحديات المجتمع 👑
          </p>
        </div>
      </div>

      <!-- الروابط -->
      <div class="space-y-5" dir="rtl">

        <!-- صف 1: Twitch + Kick -->
        <div class="tnx-links-row">
          <a
            href="https://www.twitch.tv/tnx_6"
            target="_blank"
            class="tnx-link-compact"
          >
            <span class="tnx-link-label">البث على Twitch</span>
            <div class="tnx-link-icon">
              <Icon name="tabler:brand-twitch" class="w-7 h-7 text-[#ffda6a]" />
            </div>
          </a>

          <a
            href="https://kick.com/tnx6"
            target="_blank"
            class="tnx-link-compact"
          >
            <span class="tnx-link-label">البث على Kick</span>
            <div class="tnx-link-icon">
              <Icon name="tabler:letter-k" class="w-7 h-7 text-[#ffda6a]" />
            </div>
          </a>
        </div>

        <!-- صف 2: YouTube + TikTok -->
        <div class="tnx-links-row">
          <a
            href="https://www.youtube.com/@TNX66"
            target="_blank"
            class="tnx-link-compact"
          >
            <span class="tnx-link-label">قناتي في YouTube</span>
            <div class="tnx-link-icon">
              <Icon name="tabler:brand-youtube" class="w-7 h-7 text-[#ffda6a]" />
            </div>
          </a>

          <a
            href="https://www.tiktok.com/@itstnx"
            target="_blank"
            class="tnx-link-compact"
          >
            <span class="tnx-link-label">مقاطعي في TikTok</span>
            <div class="tnx-link-icon">
              <Icon name="tabler:brand-tiktok" class="w-7 h-7 text-[#ffda6a]" />
            </div>
          </a>
        </div>

        <!-- صف 3: Instagram + Discord -->
        <div class="tnx-links-row">
          <a
            href="https://www.instagram.com/itstnx6"
            target="_blank"
            class="tnx-link-compact"
          >
            <span class="tnx-link-label">حسابي في Instagram</span>
            <div class="tnx-link-icon">
              <Icon name="tabler:brand-instagram" class="w-7 h-7 text-[#ffda6a]" />
            </div>
          </a>

          <a
            href="https://discord.gg/x7FtrfM6Sk"
            target="_blank"
            class="tnx-link-compact"
          >
            <span class="tnx-link-label">سيرفر Discord</span>
            <div class="tnx-link-icon">
              <Icon name="tabler:brand-discord" class="w-7 h-7 text-[#ffda6a]" />
            </div>
          </a>
        </div>

        <!-- صف 4: الموقع الرئيسي (بدون target="_blank") -->
        <div class="tnx-links-row">
          <a
            href="/"
            class="tnx-link-compact tnx-link-main-site"
          >
            <span class="tnx-link-label">الموقع الرئيسي</span>
            <div class="tnx-link-icon">
              <Icon name="tabler:world-www" class="w-7 h-7 text-[#ffda6a]" />
            </div>
          </a>
        </div>
      </div>

      <!-- فوتر بسيط -->
      <div class="mt-10 text-xs text-gray-400">
        © {year} TNX6 — All Rights Reserved
      </div>
    </div>
  </main>

  <style is:global>
    .tnx-ring-links{
      width: 120px;
      height: 120px;
      border-radius: 9999px;
      border: 3px solid rgba(194,146,0,0.9);
      padding: 6px;
      box-shadow: 0 0 18px rgba(194,146,0,0.25);
      background: transparent;
      overflow: hidden;
    }

    .tnx-links-row{
      display: flex;
      justify-content: center;
      gap: 3rem;
    }

    .tnx-link-compact{
      position: relative;
      display: flex;
      flex-direction: row-reverse;
      align-items: center;
      justify-content: flex-end;
      gap: 0.6rem;
      border-radius: 9999px;
      padding: 0.4rem;
      padding-inline-start: 0.4rem;
      padding-inline-end: 0.4rem;
      background: transparent;
      border: 1px solid transparent;
      color: #ffda6a;
      text-decoration: none;
      cursor: pointer;
      min-width: 0;
      transition:
        background 0.25s ease,
        border-color 0.25s ease,
        box-shadow 0.25s ease,
        padding 0.25s ease,
        transform 0.2s ease,
        opacity 0.2s ease;
    }

    .tnx-link-icon{
      width: 70px;
      height: 70px;
      border-radius: 9999px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(0,0,0,0.85);
      border: 2px solid rgba(194,146,0,0.9);
      box-shadow: 0 0 16px rgba(0,0,0,0.9);
      transition:
        background 0.25s ease,
        border-color 0.25s ease,
        box-shadow 0.25s ease,
        transform 0.2s ease;
    }

    .tnx-link-label{
      white-space: nowrap;
      max-width: 0;
      opacity: 0;
      overflow: hidden;
      font-weight: 600;
      font-size: 0.9rem;
      transition:
        max-width 0.25s ease,
        opacity 0.25s ease;
    }

    .tnx-links-row:hover .tnx-link-compact{
      opacity: 0.5;
    }

    .tnx-links-row:hover .tnx-link-compact:hover{
      opacity: 1;
    }

    .tnx-links-row:hover .tnx-link-compact:not(:hover){
      transform: translateY(6px) scale(0.9);
    }

    .tnx-link-compact:hover{
      background-image: linear-gradient(to left, #ffda6a, #c29200);
      border-color: rgba(0,0,0,0.7);
      box-shadow: 0 0 20px rgba(194,146,0,0.55);
      color: #000;
      padding-inline-start: 0.9rem;
      padding-inline-end: 0.9rem;
      transform: translateY(-6px) scale(1.08);
    }

    .tnx-link-compact:hover .tnx-link-icon{
      background: rgba(0,0,0,0.05);
      border-color: transparent;
      box-shadow: none;
      transform: scale(1.02);
    }

    .tnx-link-compact:hover .tnx-link-label{
      max-width: 260px;
      opacity: 1;
    }

    .tnx-link-compact:active{
      transform: translateY(-3px) scale(0.97);
      box-shadow: 0 0 14px rgba(194,146,0,0.6);
    }

    .tnx-link-main-site:hover{
      box-shadow: 0 0 24px rgba(255,218,106,0.7);
    }

    @media (max-width: 640px){
      .tnx-links-row{
        gap: 2rem;
      }
      .tnx-link-icon{
        width: 64px;
        height: 64px;
      }
    }
  </style>
</Layout>
