---
import Layout from '~/layouts/PageLayout.astro'

const metadata = {
  title: 'TNX6 — الروابط',
}

const year = new Date().getFullYear()
---

<Layout metadata={metadata}>
  <main class="min-h-[100vh] flex items-center justify-center px-6 py-12 text-white">
    <div class="w-full max-w-md mx-auto text-center">

      <!-- الصورة + الاسم -->
      <div class="flex flex-col items-center gap-4 mb-8">
        <div class="tnx-ring-links">
          <img
            src="/assets/images/tnx-min.jpg"
            alt="TNX Avatar"
            class="w-full h-full object-cover rounded-full"
            loading="lazy"
          />
        </div>

        <div>
          <h1 class="text-2xl font-extrabold tracking-wide">
            TNX6
          </h1>
          <p class="mt-1 text-sm text-gray-300">
            ستريمر يحب القصص، السوالف، وتحديات المجتمع 👑
          </p>
        </div>
      </div>

      <!-- الروابط على شكل أيقونات تتحول لشريط عند الـ hover -->
      <div class="space-y-4 flex flex-col items-center" dir="rtl">

        <!-- Twitch -->
        <a
          href="https://www.twitch.tv/tnx_6"
          target="_blank"
          class="tnx-link-compact"
        >
          <span class="tnx-link-label">البث على Twitch</span>
          <div class="tnx-link-icon">
            <iconify-icon icon="tabler:brand-twitch" class="text-2xl"></iconify-icon>
          </div>
        </a>

        <!-- Kick -->
        <a
          href="https://kick.com/tnx6"
          target="_blank"
          class="tnx-link-compact"
        >
          <span class="tnx-link-label">البث على Kick</span>
          <div class="tnx-link-icon">
            <iconify-icon icon="simple-icons:kick" class="text-2xl"></iconify-icon>
          </div>
        </a>

        <!-- YouTube -->
        <a
          href="https://www.youtube.com/@TNX66"
          target="_blank"
          class="tnx-link-compact"
        >
          <span class="tnx-link-label">قناتي في YouTube</span>
          <div class="tnx-link-icon">
            <iconify-icon icon="tabler:brand-youtube" class="text-2xl"></iconify-icon>
          </div>
        </a>

        <!-- TikTok -->
        <a
          href="https://www.tiktok.com/@itstnx"
          target="_blank"
          class="tnx-link-compact"
        >
          <span class="tnx-link-label">مقاطعي في TikTok</span>
          <div class="tnx-link-icon">
            <iconify-icon icon="tabler:brand-tiktok" class="text-2xl"></iconify-icon>
          </div>
        </a>

        <!-- Instagram -->
        <a
          href="https://www.instagram.com/itstnx6"
          target="_blank"
          class="tnx-link-compact"
        >
          <span class="tnx-link-label">Instagram</span>
          <div class="tnx-link-icon">
            <iconify-icon icon="tabler:brand-instagram" class="text-2xl"></iconify-icon>
          </div>
        </a>

        <!-- Discord -->
        <a
          href="https://discord.gg/x7FtrfM6Sk"
          target="_blank"
          class="tnx-link-compact"
        >
          <span class="tnx-link-label">سيرفر Discord</span>
          <div class="tnx-link-icon">
            <iconify-icon icon="tabler:brand-discord" class="text-2xl"></iconify-icon>
          </div>
        </a>

        <!-- الموقع الرئيسي -->
        <a
          href="https://tnx6.xyz"
          target="_blank"
          class="tnx-link-compact tnx-link-main-site"
        >
          <span class="tnx-link-label">الموقع الرئيسي</span>
          <div class="tnx-link-icon">
            <iconify-icon icon="tabler:world-www" class="text-2xl"></iconify-icon>
          </div>
        </a>
      </div>

      <!-- فوتر بسيط -->
      <div class="mt-8 text-xs text-gray-400">
        © {year} TNX6 — All Rights Reserved
      </div>
    </div>
  </main>

  <style is:global>
    /* حلقة الصورة */
    .tnx-ring-links{
      width: 120px;
      height: 120px;
      border-radius: 9999px;
      border: 3px solid rgba(194,146,0,0.9);
      padding: 6px;
      box-shadow: 0 0 18px rgba(194,146,0,0.25);
      background: transparent;
      overflow: hidden;
    }

    /* شكل الرابط المدمج (أيقونة فقط، ويتوسع عند الهوفر) */
    .tnx-link-compact{
      position: relative;
      display: flex;
      flex-direction: row-reverse; /* عشان الأيقونة تكون يمين والنص يسار مع RTL */
      align-items: center;
      gap: 0.6rem;
      border-radius: 9999px;
      padding: 0.35rem;
      padding-inline-start: 0.35rem;
      padding-inline-end: 0.35rem;
      background: transparent;
      border: 1px solid transparent;
      color: #ffda6a;
      text-decoration: none;
      cursor: pointer;
      transition:
        background 0.25s ease,
        border-color 0.25s ease,
        box-shadow 0.25s ease,
        padding 0.25s ease,
        transform 0.1s ease;
    }

    .tnx-link-compact:hover{
      background-image: linear-gradient(to left, #ffda6a, #c29200);
      border-color: rgba(0,0,0,0.5);
      box-shadow: 0 0 18px rgba(194,146,0,0.45);
      color: #000;
      padding-inline-start: 0.8rem;
      padding-inline-end: 0.8rem;
      transform: translateY(-1px);
    }

    .tnx-link-compact:active{
      transform: scale(0.97);
      box-shadow: 0 0 10px rgba(194,146,0,0.5);
    }

    /* دائرة الأيقونة */
    .tnx-link-icon{
      width: 52px;
      height: 52px;
      border-radius: 9999px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(0,0,0,0.7);
      border: 1px solid rgba(194,146,0,0.7);
      box-shadow: 0 0 14px rgba(0,0,0,0.8);
      transition:
        background 0.25s ease,
        border-color 0.25s ease,
        box-shadow 0.25s ease;
    }

    .tnx-link-compact:hover .tnx-link-icon{
      background: rgba(0,0,0,0.05);
      border-color: transparent;
      box-shadow: none;
    }

    /* النص المخفي، يظهر ويتوسع عند الهوفر */
    .tnx-link-label{
      white-space: nowrap;
      max-width: 0;
      opacity: 0;
      overflow: hidden;
      font-weight: 600;
      font-size: 0.9rem;
      transition:
        max-width 0.25s ease,
        opacity 0.25s ease;
    }

    .tnx-link-compact:hover .tnx-link-label{
      max-width: 260px; /* مساحة كافية للجملة */
      opacity: 1;
    }

    /* ممكن تعطي الموقع الرئيسي تميز بسيط لو حبيت */
    .tnx-link-main-site:hover{
      box-shadow: 0 0 22px rgba(255,218,106,0.65);
    }
  </style>
</Layout>
